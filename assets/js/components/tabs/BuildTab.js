import React from 'react';

const BuildTab = ({ itemId }) => {
    return (
        <div>
            <h3>Build Tab</h3>
            <p>Item ID: {itemId}</p>
            {/* Add Build tab content here */}
        </div>
    );
};

export default BuildTab;
